pipeline{

    agent any
    

    // environment {
    //     JAVA_HOME = 'C:/sdk/jdk-11.0.17'
    // }

    // parameters{
    //     string(name: 'SPEC', defaultValue: "cypress/e2e/2-advanced-examples/**.js", description: "masukan folder project cypress")
    //     choice(name: 'BROWSER', choice: ['chrome, edge, firefox'], description: "Pilih browser")
    // }

    options{
        ansiColor('xterm')
    }

    stages{
        stage('Building'){
            steps{
                // echo "Building the application"
                script{
                    env.JAVA_HOME ='C:/sdk/jdk-11.0.17'
                }
            }
        }
        stage('Testing'){
            steps{
                bat "npm i"
                // bat "npx cypress run --browser ${BROWSER} --spec ${SPEC}"
            }
        }
        stage('Deploying'){
            steps{
                echo "Deploy the application"

            }
        }
    }

    post{
        always{
            allure includeProperties: false, jdk: '', results: [[path: 'allure-results']]
        }
    }
}